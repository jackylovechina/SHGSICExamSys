<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.exam.mapper.TestMapper">
	<select id="selectTestByID" resultType="Test" parameterType="int">
		SELECT *
		FROM test_table
		WHERE id=#{id}
	</select>
	
</mapper>